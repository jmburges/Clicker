<%= form_tag "/questions/#{@question.id}/open_question", :remote => true %>
Open question for
<%= select_tag :minutes, options_for_select(Array.new(61){|i|i}) %>:
<%= select_tag :seconds, options_for_select(Array.new(61){|i|i})%>
<%= submit_tag "Open Question" %>
